task updateApiKey {
    doFirst {
        ant.replaceregexp (file: "$project.rootDir/app/src/main/res/values/api_keys.xml",
                match: "$match", flags: 'g',
                replace: "$key")
    }
}
